async function listenFilms(dataFilm) {
    const { name, compatibleFilms } = dataFilm;

    try {


    } catch (error) {
        return await (error.message)
    }
}

module.exports = { listenFilms };